import "../styles/pages/not-found-page.css";

/**
 * NotFoundPage Component
 *
 * This component renders the "404 Page Not Found" screen.
 * It is displayed when a user navigates to a route that does not exist within the application.
 *
 * Styling:
 * - The component uses styles from `not-found-page.css` to define its appearance.
 *
 * @returns {JSX.Element} The rendered NotFoundPage component.
 *
 * @example
 * return <NotFoundPage />;
 */
export default function AboutPage() {
	return <h2>404 Page Not Found</h2>;
}
